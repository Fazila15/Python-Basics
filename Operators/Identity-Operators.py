# Identity Operators gives output in boolean ( True / False )

x = [1, 2, 3]
y = [1, 2, 3]

# 'is' checks if both variables point to the same object in memory
print(x is y)
# 'is not' checks if they point to different objects
print(x is not y)